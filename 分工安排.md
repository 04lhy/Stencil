# 9-Point Stencil 并行优化 — 组内分工安排

> 组内共 9 人。1 人已完成代码，7 人协作制作 PPT，1 人负责讲解演示。

| 成员 | 分工 | 具体任务 |
| --- | --- | --- |
| 刘晗阳 | 代码开发 | 全部代码实现与优化，为 PPT 提供素材和技术答疑 |
| 王杰彬 | PPT §1 | 题目背景与问题分析 + PPT合成 + 上台讲解|
| 李润铖 | PPT §2 | 基准代码分析与性能瓶颈诊断 |
| 李明喆 | PPT §3 | 编译优化与 OpenMP 多线程并行化 |
| 王向光 | PPT §4 | AVX2 SIMD 手动向量化 |
| 陈宏 | PPT §5 | 访存优化：Stream Store 与 Block Kernel |
| 唐志康 | PPT §6 | IPCC 参考代码对比分析 |
| 肖清峰 | PPT §7 | 双平台适配与正确性验证 |
| 覃禹嘉 | 讲解演示 | 串讲全部 PPT + 现场 Demo + Q&A |

***

## 角色总览

| 角色         | 人数 | 成员         | 职责                        |
| ---------- | -- | ---------- | ------------------------- |
| **代码开发**   | 1  | （已完成）      | 全部代码实现与优化，为 PPT 提供素材和技术答疑 |
| **PPT 制作** | 7  | §1-§7 每人一章 | 独立制作 4-6 页幻灯片，直接分析本仓库代码   |
| **讲解演示**   | 1  | （演讲者）      | 串讲全部内容                    |

***

## PPT 章节分工（7 人，一人一章）

> 每个章节都列出了**可直接分析的本仓库代码文件**，建议各位先跑通 `make all && make run` 再看代码。

***

### §1 题目背景与问题分析

**负责：** A 同学

**内容要点：**

- 什么是 9-point stencil：用 3×3 邻域像素做加权模糊计算
- 公式：`out[row][col] = 8*center - sum(8 outer neighbors)` + clamp \[0,255]
- 赛题约束：nTrials 循环不可拆分并行、计时不含 I/O、误差 < 1e-5
- 输入图像：IPCC.png（10410 × 5905 ≈ 61M 像素，8-bit 灰度）
- 优化目标：从秒级压到毫秒级

**可直接分析的代码文件：**

- [实践题目.md](实践题目.md) — 原始赛题要求
- [stencil/src/main.cc](stencil/src/main.cc) — 主程序入口，了解 nTrials 计时框架
- [stencil/src/stencil.h](stencil/src/stencil.h) — `ApplyStencil` 模板声明
- `stencil/IPCC.png` — 输入图像

**建议页数：** 4-5 页

***

### §2 基准代码分析与性能瓶颈诊断

**负责：** B 同学

**内容要点：**

- 基准代码调用链：`main.cc` → `ApplyStencil()` → `image.cc`（读/写 PNG）
- 原始 `stencil.cc` 的 5 大瓶颈分析：
  1. **串行执行** — 无任何并行化，仅用单核
  2. **标量计算** — 逐元素 float 运算，未利用 SIMD
  3. **循环命名混乱** — 外层 j（列）、内层 i（行），对 cache 不友好
  4. **冗余索引计算** — 每次迭代计算 9 个 2D 索引（`(i-1)*width + (j-1)` 等）
  5. **Clamp 分支** — `val < 0 ? 0 : (val > 255 ? 255 : val)`，分支预测失败开销大
- 双平台基线：
  - 平台 A（Arrow Lake WSL2）：纯标量串行 O0 → **8529.8 ms**
  - 平台 B（Raptor Lake 原生）：O0 + AVX2 + 4T → **349.6 ms**
- Makefile 多目标编译框架：[stencil/Makefile](stencil/Makefile)

**可直接分析的代码文件：**

- [stencil/src/main.cc](stencil/src/main.cc) — 计时框架
- [stencil/src/stencil.cc](stencil/src/stencil.cc) — 当前优化版（反向理解瓶颈）
- [stencil/src/image.cc](stencil/src/image.cc) — I/O 流程
- [优化记录.md](stencil/优化记录.md) §二 — 环境与基线数据

**建议页数：** 5-6 页

***

### §3 编译优化与 OpenMP 多线程并行化

**负责：** C 同学

**内容要点：**

- 编译选项演进（不修改源码即可获得的基础加速）：
  - `-O0` → `-O3` → `-Ofast -march=native -mavx2 -mfma`
  - 效果仅 \~1.5× → GCC 自动向量化对此计算模式有限
  - 引出：**必须手动 SIMD 向量化**
- OpenMP 并行化方案：
  - `#pragma omp parallel for schedule(static)` 外层行循环
  - 循环顺序：外层 i（行）、内层 j（列） → Row-major cache 友好
  - 线程数扫描实验：4T / 8T / 12T / 16T / 20T / 24T
  - 最优 20T（Arrow Lake）/ 16T（Raptor Lake）
- 线程亲和性：`OMP_PROC_BIND=spread` vs `close`
- 成果：编译优化 \~1.5× + OpenMP \~10.7× → 总加速 \~16×（8529.8 → \~796ms(4T) / \~160ms(20T)）

**可直接分析的代码文件：**

- [stencil/Makefile](stencil/Makefile) — CXXFLAGS\_OPT / RUN\_ENV 变量
- [stencil/src/stencil.cc](stencil/src/stencil.cc) 第 33 行 — `#pragma omp parallel for`
- [优化记录.md](stencil/优化记录.md) §五（阶段 0-2）

**建议页数：** 4-5 页

***

### §4 AVX2 SIMD 手动向量化

**负责：** D 同学

**内容要点：**

- 为什么手动向量化（GCC 对此计算模式的自动向量化效果差）
- AVX2 基础：256-bit YMM 寄存器，一次处理 **8 个 float**
- 核心 intrinsics 指令及在本项目中的用法：
  - `_mm256_loadu_ps` — 非对齐加载 9 个邻居
  - `_mm256_fmsub_ps` — 融合乘减：`8*center - sum` 一条指令完成
  - `_mm256_add_ps` / `_mm256_sub_ps` — 向量加减
  - `_mm256_max_ps` / `_mm256_min_ps` — 无分支 clamp \[0,255]
- 公式变换：`9*center - sum(9 neighbors) = 8*center - sum(8 outer neighbors)`
- 寄存器操作序列图示（load → add → fmsub → max → min → store）
- 边界处理：前置标量序言（对齐 32 字节）+ 尾部标量循环

**可直接分析的代码文件：**

- [stencil/src/stencil.cc](stencil/src/stencil.cc) 第 63-109 行 — SIMD 主循环
- [stencil/src/kernel\_avx2.h](stencil/src/kernel_avx2.h) — AVX2 kernel 集合（row + block）
- [stencil/src/stencil\_avx2.cc](stencil/src/stencil_avx2.cc) — 逐行 kernel 包装
- [stencil/src/stencil\_block.cc](stencil/src/stencil_block.cc) — 分块 kernel 包装
- [优化记录.md](stencil/优化记录.md) §六 — AVX2 与访存优化

**建议页数：** 5-6 页

***

### §5 访存优化：Stream Store 与 Block Kernel

**负责：** E 同学

**内容要点：**

- **VTune 分析结论**：程序是 Memory Bound（访存受限），不是 Compute Bound
- **Stream Store（非临时写入）**：
  - 问题：`_mm256_storeu_ps` 触发 Write-Allocate → 污染 cache
  - 方案：`_mm256_stream_ps` 绕过 cache，直接写内存
  - 对齐要求：目标地址 32 字节对齐 → 每行前置标量序言
  - 贡献 \~2.1×（平台 A）
- **软件预取**：`_mm_prefetch(addr, _MM_HINT_T0)` 提前加载到 L1
  - 效果有限（\~1ms）→ 硬件预取器已足够
- **Block Kernel（读 N+2 行算 N 行）**：
  - 读 6 行算 4 行（BATCH\_SIZE=4），寄存器复用减少重复 load
  - 在 EPYC 获奖平台上有效，但本仓库超宽图像（10410 列）上**反而更慢**
  - 原因：1) 额外 1.5× 内存读取 2) 无法用 stream store 3) 逐行 kernel 已有最优 cache 局部性
- 成果：\~67ms（平台 A），stream store 贡献 \~2.1×

**可直接分析的代码文件：**

- [stencil/src/stencil.cc](stencil/src/stencil.cc) 第 44-61 行 — 对齐序言 + stream store
- [stencil/src/kernel\_avx2.h](stencil/src/kernel_avx2.h) — block kernel 实现
- [stencil/src/stencil\_block.cc](stencil/src/stencil_block.cc) — block kernel 包装
- [优化记录.md](stencil/优化记录.md) §6.1（为什么 block kernel 更慢）、§6.4

**建议页数：** 4-5 页

***

### §6 IPCC 参考代码对比分析

**负责：** F 同学

> **这是最重要的章节之一**。需要深入对比 IPCC 获奖代码（[IPCC\_stencil/src/opt\_kernel.h](IPCC_stencil/src/opt_kernel.h)）与本仓库代码（[stencil.cc](stencil/src/stencil.cc)），分析差异和关键洞察。

**内容要点：**

**6.1 代码结构对比：**

| 维度        | IPCC opt\_kernel.h                     | 本项目 stencil.cc             |
| --------- | -------------------------------------- | -------------------------- |
| 索引方式      | **1D 线性化** `in[index]`                 | 逐行指针 `r0+j, r1+j, r2+j`    |
| 对齐处理      | **1 次前置对齐**                            | 每行对齐检测（5905 次）             |
| 加法策略      | **分组加法**（4 独立子树）                       | **分组加法**（已集成 IPCC 策略）      |
| SIMD 宽度   | 8 floats (AVX2)                        | 8 floats (AVX2)            |
| 软件预取      | 无                                      | `_mm_prefetch` T0（3 条）     |
| OpenMP 调度 | 手动 `distribute_row` + pthread affinity | `#pragma omp parallel for` |

**6.2 核心差异——分组加法 vs 顺序累加（依赖链深度分析）：**

原始顺序累加（依赖链 \~11 级）：

```
sum  = TL         // load
sum += TC         // +1 级
sum += TR         // +1 级
... 共 9 次串行累加 ...
val = 9*C - sum
```

IPCC 分组加法（依赖链 \~5 级）：

```
val1 = TL + TC          (组1, load + 1 add)
val2 = TR + ML          (组2, load + 1 add)   } 4 条链
val3 = 8*C - MR         (组3, fmsub)          } 可以
val4 = BL + BC          (组4, load + 1 add)   } 并行发射
val5 = BR               (组5, load only)

合并（3 级加法树）:
v10 = val1 + val2       // TL+TC+TR+ML
v11 = val3 - val4       // 8*center-MR-BL-BC
v20 = v10 + val5        // TL+TC+TR+ML+BR
val = v11 - v20          // 8*center - sum_of_8_neighbors
```

**6.3 指令级并行 (ILP) 原理：**

- 4 条独立加法链无数据依赖 → CPU 乱序执行引擎同时发射
- 依赖链从 11 级降到 5 级 → 指令流水线停顿减少 >50%
- 平台 A（Arrow Lake）受益更大（宽发射架构），提升 \~7%
- 平台 B（Raptor Lake）乱序窗口较窄，提升 \~1-2%

**6.4 公式等价性证明：**

```
本项目: 9*C - (TL+TC+TR+ML+C+MR+BL+BC+BR) = 8*C - sum_of_8_neighbors
IPCC:   8*C - (TL+TC+TR+ML+MR+BL+BC+BR)   = 8*C - sum_of_8_neighbors
→ 数学等价，加法分组方式不同
```

**6.5 关键洞察：**

- 1D 线性化一次前置对齐 > 逐行指针每行对齐检测（但逐行代码更易读）
- 分组加法**是本仓库从 IPCC 学到的最关键技术**，已集成到 stencil.cc
- 编译器差异：IPCC 用 Intel ICC，本仓库用 GCC（自动向量化策略不同）

**可直接分析的代码文件：**

- ⭐ [IPCC\_stencil/src/opt\_kernel.h](IPCC_stencil/src/opt_kernel.h) — IPCC 获奖 kernel（83 行，核心算法）
- ⭐ [stencil/src/stencil.cc](stencil/src/stencil.cc) — 本仓库默认实现（已集成分组加法）
- [IPCC\_stencil/src/my\_kernel.h](IPCC_stencil/src/my_kernel.h) — IPCC 另一 kernel 变体
- [IPCC\_stencil/src/kernel\_r6c4\_avx2.h](IPCC_stencil/src/kernel_r6c4_avx2.h) — IPCC 分块 kernel
- [优化记录.md](stencil/优化记录.md) §6.6 — IPCC vs 本项目逐项对比
- [优化记录.md](stencil/优化记录.md) §9.2 — 分组加法效果数据

**建议页数：** 5-6 页

***

### §7 双平台适配与正确性验证

**负责：** G 同学

**内容要点：**

**7.1 双平台硬件对比：**

| <br />    | 平台 A                    | 平台 B                |
| --------- | ----------------------- | ------------------- |
| CPU       | Core Ultra 9 275HX      | i9-13900H           |
| 架构        | Arrow Lake (P+E hybrid) | Raptor Lake (6P+8E) |
| 核数        | 24C/24T                 | 14C/20T             |
| SIMD      | AVX2 + FMA              | AVX2 + FMA          |
| 系统        | Ubuntu 20.04 WSL2       | Ubuntu 22.04 原生     |
| 编译器       | GCC 9.4.0               | GCC 11.4.0          |
| 最优时间      | 62.5ms                  | 86.9ms              |
| P-core 可见 | ❌ WSL2 不暴露              | ✅ 可见，可绑定            |

**7.2 P-core 精确绑定（平台 B 独有）：**

- 原生 Linux 暴露 hybrid 拓扑：P-core (CPU 0-11), E-core (CPU 12-19)
- `taskset -c 0-11` + `OMP_PROC_BIND=close` → P-core 12T
- P-core 12T (88.9ms) > 全核 20T (93.8ms)，快 \~5%
- E-core 比 P-core 慢 \~2.8×/每核
- WSL2 无法做此优化（不暴露 topology）

**7.3 NUMA 分析：**

- 两个平台都是单路 CPU → 无跨 socket 开销
- IPCC 获奖方案的 mmap + First Touch 策略在单路上无额外收益，**可跳过**

**7.4 多图像交叉验证：**

- 用 5 张不同尺寸图像（1024\~10410 像素）验证正确性
- 方法：优化版 (Ofast) vs 调试版 (O0) 输出逐元素对比
- 结果：全部 ✅
- 命令：`make validate-multi`

**7.5 未实现优化：**

- AVX-512：两个平台均不支持
- 大页内存：madvise(MADV\_HUGEPAGE) 已添加，THP 未触发

**可直接分析的代码文件：**

- [stencil/Makefile](stencil/Makefile) — P\_CORE / run-pcore / validate-multi 目标
- [stencil/src/image.cc](stencil/src/image.cc) 第 14、70 行 — madvise 大页内存
- [实践计划.md](实践计划.md) §一 — 双平台环境详表
- [优化记录.md](stencil/优化记录.md) §9 — 新增优化详解
- [优化记录.md](stencil/优化记录.md) §10 — 平台对比分析

**建议页数：** 4-5 页

***

## 讲解演示（1 人）

**负责：** H 同学

**职责：**

1. **串讲 PPT**：整合 7 个章节，用统一叙事线串联——"从 8.5 秒到 62 毫秒"
2. **现场 Demo**（在当前机器上运行）：
   ```bash
   cd stencil
   make all                     # 编译
   make run                     # 16T 全核运行
   make run-pcore               # P-core 12T 最优配置
   make check                   # 正确性验证 ✅
   make validate-multi          # 5 图像交叉验证
   make bench-all               # 全量性能对比
   ```
3. **Q\&A 准备**：提前熟悉各章节细节，与每位 PPT 制作者确认技术要点

**建议叙事线：**

```
§1 这是什么题 ──→ §2 原始代码瓶颈在哪 ──→ §3 编译+并行提速 16×
────→ §4 AVX2 向量化再提速 6× ──→ §5 访存优化再提速 2×
────→ §6 IPCC 参选代码对比，关键差异在哪 ──→ §7 不同平台怎么适配
────→ 总结：从 8.5s 到 62ms，136× 加速
```

***

## 协作规范

| 事项       | 要求                                                             |
| -------- | -------------------------------------------------------------- |
| **模板统一** | 统一使用同一 PPT 模板（封面、字体、配色），由 H 同学创建空白模板分发                         |
| **代码引用** | 代码片段截图并标注文件来源和行号，推荐 VS Code 截图（含行号）                            |
| **数据一致** | 所有性能数据必须引用 [优化记录.md](stencil/优化记录.md) 和 [README.md](README.md) |
| **图表规范** | 性能演进图、依赖链示意图、架构对比图 — draw\.io / PPT 原生 / Visio                 |
| **图片素材** | 从 `stencil/IPCC.png`、`stencil/test_*.png` 截取                   |
| **交付截止** | 每人最迟 \[日期] 前将 PPT 章节发给 H 同学整合                                  |
| **答疑支持** | 代码作者负责解答各 PPT 制作者的技术问题，建议先通读对应代码文件                             |
 |

