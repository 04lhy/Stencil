
这份报告详细记录了针对 9-point stencil 算法从基准程序到最终实现近 700 倍加速的完整优化路径。对于代码 Agent 而言，这是一份非常清晰的“性能优化行动指南”。

以下是基于文档内容的优化思路总结，你可以直接参考这些步骤来指导代码的迭代与重构：

### 🚀 核心优化路径总结

整个优化过程遵循了 **“编译优化 -> 并行化 -> 向量化 -> 访存优化 -> 架构感知优化”** 的层层递进逻辑。

#### 1. 基础编译优化 (Baseline)
*   **动作**：更换高性能编译器并开启高级优化选项。
*   **具体配置**：使用 Intel C++ Compiler (`icpc`)，开启 `-O3` (最高级优化)、`-ipo` (过程间优化)、`-xHost` (自动检测CPU指令集如AVX)、`-fp-model fast=2` (浮点运算优化)。
*   **收益**：相比 GCC 提升约 1 倍，相比原始基准提升约 4 倍。

#### 2. 并行化 (Parallelization)
*   **动作**：引入 OpenMP 进行多线程并行计算。
*   **关键点**：
    *   使用 `#pragma omp parallel for`。
    *   **注意循环顺序**：外层循环遍历行，内层循环遍历列，以保证内存访问的局部性（Row-major order）。
    *   线程数设置为物理核心数（如 64 线程）。

#### 3. 向量化 (Vectorization)
*   **动作**：利用 SIMD 指令集（AVX2）手动向量化内层循环。
*   **实现细节**：
    *   使用 Intrinsics 接口（如 `_mm256_loadu_ps`, `_mm256_fmadd_ps`）。
    *   **数据对齐**：注意处理边界情况，尾部不足 8 个 float 的数据需单独处理。
    *   **计算逻辑**：将 9 个点的加减运算转化为向量运算，一次性处理 8 个像素点。

#### 4. 访存瓶颈突破 (Memory Access Optimization)
这是性能提升最关键的阶段，通过 VTune 分析发现程序是“访存受限” (Memory Bound)。

*   **优化 A：非临时写入 (Stream Stores)**
    *   **问题**：大量写入操作导致缓存污染（Cache Pollution）。
    *   **动作**：将普通的 `_mm256_storeu_ps` 替换为 `_mm256_stream_ps`。
    *   **原理**：绕过 Cache，直接写入内存，减少读-修改-写带来的缓存开销。
    *   **注意**：使用 stream store 通常要求内存地址对齐。

*   **优化 B：NUMA 亲和性优化 (NUMA Awareness)**
    *   **问题**：在多路 CPU (Multi-socket) 系统中，线程可能访问了远程节点的内存，导致跨 CPU 通信延迟极高。
    *   **动作**：
        1.  使用 `mmap` 替代 `malloc` 分配内存（设置 `MAP_PRIVATE`）。
        2.  在 OpenMP 并行区域内进行首次内存访问（First Touch Policy），确保内存页分配在访问该内存的线程所在的 NUMA 节点上。
    *   **收益**：解决了跨 Socket 通信瓶颈，带宽翻倍，性能再次大幅提升。

---

### 🛠️ 给代码 Agent 的执行建议

如果你需要 Agent 协助优化代码，可以按照以下 Prompt 结构进行交互：

1.  **第一步（环境配置）**：
    > "请检查当前的编译选项，确保开启了 `-O3` 和 `-march=native` (或 `-xHost`)。如果使用的是 GCC，请尝试评估切换到 ICC/ICX 的可行性。"

2.  **第二步（并行与向量）**：
    > "请对核心计算循环应用 OpenMP 并行化。随后，请使用 AVX2 Intrinsics 重写内层循环，实现 8 路浮点数并行计算。请处理好数组越界和尾部数据的标量计算逻辑。"

3.  **第三步（高级访存）**：
    > "检测到写入操作频繁，请将向量存储指令改为非临时存储（Non-temporal store / Streaming store），以避免写分配带来的缓存污染。同时，请检查内存分配方式，尝试实现 'First Touch' 策略以优化 NUMA 性能。"

### 📊 优化效果对比表

| 优化阶段 | 关键技术点 | 耗时 (参考) | 加速比 (相对基准) |
| :--- | :--- | :--- | :--- |
| **基准** | 串行，无优化 | ~10,989 ms | 1.0x |
| **编译优化** | ICC, -O3, -ipo | ~2,628 ms | ~4.1x |
| **并行+向量** | OpenMP, AVX2 | ~60.7 ms | ~181x |
| **访存优化** | Stream Write | ~27.0 ms | ~407x |
| **NUMA优化** | mmap, First Touch | **15.8 ms** | **695.5x** |

这个路径非常清晰地展示了如何从算法逻辑优化深入到硬件架构层面的优化。

---

## 🖥️ 本仓库实际实现

### 平台概览

本仓库在两个平台上进行了完整的优化和测试：

| 项目 | 平台 A (Arrow Lake) | 平台 B (Raptor Lake) |
|------|--------------------|--------------------|
| CPU | Core Ultra 9 275HX | Core i9-13900H |
| 核心 | 24C/24T (P+E hybrid) | 14C/20T (6P+8E) |
| 系统 | Ubuntu 20.04 WSL2 | Ubuntu 22.04 原生 |
| 编译器 | GCC 9.4.0 | GCC 11.4.0 |
| 最优时间 | 62.5 ms | 86.9 ms |
| 加速比 (vs 纯标量) | 136.5× | — |
| GPU | — | RTX 4070 (8GB) |

### 平台 B: i9-13900H 原生 Linux — 最新结果

#### 性能对比

| 版本 | 时间 | 技术栈 |
|------|------|--------|
| **stencil_simple** | **86.9 ms** | IPCC 分组加法 + Stream + P-core 12T |
| stencil | 88.9 ms | 分组加法 + Stream + P-core 12T close |
| stencil_avx2 | 89.0 ms | 顺序累加 + 2x Unroll + Stream |
| stencil_block | 142.5 ms | 读6算4 分块 kernel |
| stencil_dbg | 349.6 ms | O0, 4T, AVX2 intrinsics |

#### 最佳配置

```bash
# 编译
make all

# 运行（最优配置：P-core 12 线程，close 绑定）
OMP_NUM_THREADS=12 OMP_PROC_BIND=close taskset -c 0-11 ./bin/stencil IPCC.png
```

#### 新增优化成果

| 优化项 | 状态 | 效果 |
|--------|------|------|
| ✅ P-core 精确绑定 | 完成 | P-core 12T > 全核 20T (快 ~5%) |
| ✅ IPCC 分组加法 | 完成 | 替代顺序累加，依赖链 ~5 级 |
| ✅ 多图像交叉验证 | 完成 | 5 图像 × 2 版本 全部一致 |
| ✅ stencil_simple 修复 | 完成 | 修复线程划分溢出 bug |
| ⚠️ 大页内存 | 部分 | madvise 已添加，THP 未触发 |
| ❌ AVX-512 | 不可行 | i9-13900H 不支持 |

### 平台 A: Arrow Lake WSL2 — 历史记录

#### 硬件对比 (vs EPYC 获奖平台)

| 指标 | EPYC 7452×2 | Arrow Lake 275HX | 比值 |
|------|-------------|------------------|------|
| 核心数 | 64C (2 sockets) | 24C (1 socket) | 2.7× |
| 内存通道 | 16 (8×2) | 2 | 8× |
| 内存带宽 | ~200 GB/s | ~90 GB/s | 2.2× |
| 最优时间 | 15.8 ms | 62.5 ms | 4.0× |
| 加速比 | 695.5× | 136.5× | 5.1× |

#### 本仓库核心发现

1. **NUMA 优化可跳过**：单路 CPU 无跨 socket 通信开销
2. **分块 kernel 不适用超宽图像**：10410 列的宽图像已充分利用 cache 局部性，读 6 算 4 的额外 load 反而拖慢性能
3. **Hybrid 架构需调节线程数**：P-core + E-core 混合架构下，20 线程 > 24 线程 (平台A) / 12T P-core > 20T all (平台B)
4. **分组加法优于顺序累加**：IPCC 参考代码的分组加法策略降低依赖链深度
5. **GCC 自动向量化已足够**：在无 ICC 编译器的情况下，GCC `-Ofast -march=native` 配合手动 AVX2 可达同等水平
6. **P-core 绑定有效**：原生 Linux 上精确绑定 P-core 优于全核运行

### 实际性能演进

| 阶段 | 平台 A (Arrow Lake) | 平台 B (Raptor Lake) | 技术 |
|------|--------------------|--------------------|------|
| 0-baseline | 8529.8 ms (纯标量) | 349.6 ms (O0 4T) | 基准 |
| 1-编译优化 | 5855.6 ms (1.5×) | — | -O3 -ffast-math |
| 2-OpenMP | 796.4 ms (10.7×) | ~160 ms | 多线程并行 |
| 3-AVX2 SIMD | 138.2 ms (61.7×) | — | 手动向量化 |
| 4-Stream Store | ~67 ms (~127×) | 93.4 ms | 非临时写入 |
| 5-分组加法 | 62.5 ms (136.5×) | **88.9 ms** | IPCC 分组加法 |
| ★ P-core 绑定 | — | **86.9 ms** | taskset 精确绑定 |

详见 [优化记录.md](stencil/优化记录.md) 获取完整的逐项对比和代码分析。