# 9-point Stencil 优化记录

## 一、最终性能总览

### ══════ 平台 A: Arrow Lake (WSL2, 旧) ══════

| 指标 | 值 |
|------|-----|
| 基准 (O0 serial) | **8529.8 ms** |
| 优化后 | **~67.1 ms** (AVX2+OMP+Stream+2x Unroll) |
| **加速比** | **~127.2x** |
| IPCC 参考代码 | **~62.5 ms** (加速比 ~136.5x) |
| 最佳配置 | OMP_NUM_THREADS=20, OMP_PROC_BIND=spread |

### ══════ 平台 B: i9-13900H (原生 Linux, 新) ══════

| 指标 | 值 |
|------|-----|
| 基准 (O0 4T) | **349.6 ms** |
| 优化后 | **~88.9 ms** (分组加法 + Stream + P-core 12T) |
| **加速比 vs Debug** | **~3.9x** |
| IPCC 分组加法贡献 | ~2ms (91.9ms vs 93.8ms sequential) |
| 正确性 | ✅ 5 图像交叉验证通过 |
| 最佳配置 | P-core 12T close (`taskset -c 0-11`) |

> **注意**: 平台 B 的 debug 基准包含 AVX2 intrinsics + OpenMP (O0 编译)，因此加速比数值（3.9x）不可与平台 A 的纯标量基准加速比（127x）直接比较。平台 A 的纯标量串行基准为 8529.8ms，是真正的 0 优化起点。

### 平台 B 性能演进轨迹

| 阶段 | 总时间 | 技术 |
|------|--------|------|
| 0-debug | 349.6ms | O0, 4T, AVX2 intrinsics |
| 1-Ofast+OMP | ~160ms | -Ofast, 4T |
| 2-OMP 16T | 93.4ms | OpenMP 16T spread |
| 3-分组加法 | 91.9ms | IPCC 分组加法策略 |
| 4-P-core 绑定 | **~88.9ms** | P-core 12T close |

### 平台 A 性能演进轨迹

| 阶段 | 总时间 | 加速比 | 技术 |
|------|--------|--------|------|
| 0-baseline | 8529.8ms | 1.0x | O0, serial, scalar |
| 1-O3 | 5855.6ms | 1.5x | -O3 -ffast-math |
| 2-OMP | 796.4ms | 10.7x | OpenMP 4 threads |
| 3-AVX2 | 138.2ms | 61.7x | + manual AVX2 SIMD |
| 4-Stream | ~67ms | ~127x | + stream store (non-temporal write) |
| 5-2x Unroll | **~66.8ms** | **~127.7x** | + 2x loop unrolling + prefetch |

---

## 二、环境信息

### 平台 A (旧)

| 项目 | 值 |
|------|-----|
| CPU | Intel Core Ultra 9 275HX (Arrow Lake-HX) |
| 核心 | 24 核 24 线程 (P-core + E-core 混合) |
| SIMD | AVX2 + FMA（无 AVX-512） |
| 编译器 | GCC 9.4.0 |
| 系统 | Ubuntu 20.04 on WSL2 |
| 内存 | 16 GB |

### 平台 B (新)

| 项目 | 值 |
|------|-----|
| CPU | Intel Core i9-13900H (Raptor Lake-H) |
| 核心 | 6P+8E = 14C/20T (P-core: 0-11, E-core: 12-19) |
| GPU | NVIDIA GeForce RTX 4070 (8GB, CUDA 13.0) |
| SIMD | AVX2 + FMA + AVX_VNNI（无 AVX-512） |
| 编译器 | GCC 11.4.0 |
| 系统 | Ubuntu 22.04 (原生 Linux, 非 WSL) |
| 内核 | 6.8.0-110-generic |
| 内存 | 15 GB |
| 图像尺寸 | 10410 × 5905 ≈ 61M 像素 |

---

## 三、文件结构

```
stencil/
├── IPCC.png              # 输入图像（不可修改）
├── check.png             # 验证图像（不可修改）
├── check.txt             # 验证数据（不可修改）
├── Makefile              # 编译脚本 (多目标)
├── bin/
│   ├── stencil           # ★ 默认优化版 (2x unroll + stream)
│   ├── stencil_avx2      # 逐行 AVX2 kernel (来自 kernel_avx2.h)
│   ├── stencil_block     # 分块 kernel (读 6 行算 4 行)
│   └── stencil_dbg       # 调试版 (O0)
├── include/
│   ├── png.h / pngconf.h / pnglibconf.h
├── lib/
│   ├── libpng16.a / libz.a
└── src/
    ├── main.cc           # 主程序入口（计时部分不可修改）
    ├── image.h / image.cc # 图像 I/O
    ├── stencil.h         # ApplyStencil 模板声明
    ├── stencil.cc        # ★ 默认优化实现 (2x unroll + stream + prefetch)
    ├── kernel_avx2.h     # AVX2 优化 kernel 集合 (row / block)
    ├── stencil_avx2.cc   # kernel_row_avx2 包装
    ├── stencil_block.cc  # kernel_block_avx2 包装
    └── stencil_simple.cc # 1D 线性化实验版 (当前有正确性 bug)
```

---

## 四、编译与运行

### 编译默认优化版
```bash
make all    # 编译 bin/stencil (2x unroll + stream + prefetch)
```

### 编译其他版本
```bash
make avx2    # 逐行 kernel 版本
make block   # 分块 kernel 版本 (仅适用于窄图像)
make debug   # 调试版本 (O0)
```

### 运行
```bash
make run     # OMP_NUM_THREADS=20 OMP_PROC_BIND=spread
make check   # 验证 data.txt == check.txt
```

### 性能对比
```bash
make bench-all  # 全量对比所有版本
make scan-threads  # 扫描最优线程数
```

### 关键编译选项
- `-Ofast`: 最高级优化（含 -ffast-math）
- `-march=native`: 自动检测 CPU 指令集
- `-mavx2 -mfma`: AVX2 + FMA 指令集
- `-fopenmp`: OpenMP 多线程
- `-no-pie`: 静态库链接必需

---

## 五、各版本性能对比 (当前硬件)

| 版本 | 总时间 | 加速比 | 关键技术 | 正确性 |
|------|--------|--------|----------|--------|
| **★ IPCC reference** | **62.5ms** | **136.5x** | 1D linear + AVX2 + stream (分组加法) | ✅ |
| stencil_simple | 64.1ms | 133.1x | 1D 线性化 (对标 IPCC) | ❌ bug |
| stencil_avx2 | 65.4ms | 130.4x | 逐行 kernel_row_avx2 | ✅ |
| stencil (默认) | **67.1ms** | **127.2x** | 2x unroll + stream + prefetch | ✅ |
| stencil_block | 113.4ms | 75.2x | 读6算4 数据复用 | ✅ |

> 测试条件: OMP_NUM_THREADS=20, OMP_PROC_BIND=spread, 3 次运行取最优值。各版本详细差异见 [六.6 节](#66-ipcc-参考实现-vs-本项目实现对比)。

---

## 六、关键技术决策

### 6.1 为什么 block kernel (读6算4) 在本环境更慢？

参考代码的"读 6 行算 4 行"策略在 EPYC 双路上有效，但在本环境 (Arrow Lake + 超宽图像) 反而慢：

1. **图像太宽 (10410 列)**：每行 ~41.6KB，3 行 ~125KB 轻松放入 L2 cache (2MB)
   → 逐行 kernel 已有最优 cache 局部性，每行只从内存加载一次

2. **Block kernel 读更多数据**：6/4 = 1.5x 内存读取 vs 逐行的 ~1x（cache 命中后）
   → 数据复用收益被额外 load 抵消

3. **Block kernel 无法用 stream store**：多行输出对齐各不相同，只能用 storeu
   → 写分配 (write-allocate) 污染 cache

4. **适用场景**：block kernel 适用于**窄图像**（行宽 >> cache line 时减少重复加载）

### 6.2 为什么 20 线程 > 24 线程？

Core Ultra 9 275HX 有 P-core（性能核）+ E-core（能效核）混合架构：
- 所有 24 核一起跑，E-core 慢拖累整体
- 20 线程是最佳平衡点（覆盖所有 P-core + 部分 E-core）
- WSL2 不暴露 hybrid 拓扑，无法精准绑定 P-core

### 6.3 为什么 OMP_PROC_BIND=spread > close？

- `spread`: 线程分散到不同物理核心，减少共享资源竞争
- `close`: 线程集中到相邻核心（可能是相邻 HT 线程），争抢 L1/L2 cache
- Arrow Lake 的 P-core 和 E-core 集群分属不同区域，spread 能更好地利用

### 6.4 2x 循环展开 + 累加式加载

关键优化：边 load 边 sum，而非加载全部 9 个向量后再计算：
```cpp
__m256 mc  = _mm256_loadu_ps(r1 + j);       // center (保留)
__m256 sum = _mm256_loadu_ps(r0 + j - 1);   // TL
sum = _mm256_add_ps(sum, _mm256_loadu_ps(r0 + j));      // + TC
// ... 边 load 边累加 ...
```
寄存器占用仅 2-3 YMM/batch，2x 展开 (16 元素/迭代) 不溢出 16 YMM 寄存器。

### 6.5 对标 IPCC 的差异分析

| 对比项 | IPCC (EPYC 7452×2) | 本实现 (Arrow Lake) |
|--------|---------------------|---------------------|
| 最终性能 | 15.8ms (64核) | 66.8ms (20线程) |
| 加速比 | 695.5x | 127.7x |
| 核数 | 64 | 24 (20 有效) |
| 每核效率 | 15.8×64 = 1011 core-ms | 66.8×20 = 1336 core-ms |
| 内存带宽 | 8-ch DDR4 ≈ 200 GB/s | 2-ch DDR5 ≈ 90 GB/s |

每核效率接近（1336 vs 1011 core-ms），差异主要来自硬件平台（核数、内存通道数）。

### 6.6 IPCC 参考实现 vs 本项目实现 对比

IPCC 官方优化代码 ([opt_kernel.h](../IPCC_stencil/src/opt_kernel.h)) 在相同硬件上跑到 **62.5ms**，比本项目最优版 (67.1ms) 快 **7%**。以下是逐项对比分析。

#### 6.6.1 代码结构对比

```
                            IPCC opt_kernel             本项目 stencil.cc
─────────────────────────────────────────────────────────────────────────────
索引方式                      1D 线性化 in[index]          逐行指针 r0+j, r1+j, r2+j
对齐处理                      一次前置对齐循环               每行检测 + 标量序言
SIMD 宽度                     8 floats (AVX2)              8 floats × 2x 展开 = 16
公式                          fmsub(8, center, MR)         fmsub(9, center, sum)
加法分组                      独立子树 (ILP 友好)           顺序累加链
Stream store                  ✅                            ✅
软件预取                      ❌                            ✅ _mm_prefetch T0
OpenMP 调度                   手动分段 (countPerThread)     #pragma omp parallel for
Scalar tail                   循环 + 寄存器 mask            标量循环逐元素
```

#### 6.6.2 加法链对比 (核心性能差异)

**IPCC 分组加法链**（4 个独立子树 → 合并）：
```cpp
// 4 条独立加法链，可并行发射
val1 = TL + TC                    // 组1: 上排左+中
val2 = TR + ML                    // 组2: 上排右+中排左
val3 = 8.0 * center - MR          // 组3: fmsub 中心-中排右
val4 = BL + BC                    // 组4: 下排左+中
val5 = BR                         // 组5: 下排右 (load only)

// 合并阶段 — 只需 3 次加法
val10 = val1 + val2               // TL+TC+TR+ML
val11 = val3 - val4               // 8*center-MR-BL-BC
val20 = val10 + val5              // TL+TC+TR+ML+BR
val   = val11 - val20             // 8*center - sum_of_8_neighbors
```
→ 依赖链深度: **load → 2 add → 1 fmsub → clamp → stream** (约 5 级)

**本项目顺序累加链**：
```cpp
sum = TL
sum += TC
sum += TR
sum += ML
sum += center     // ← 注意: center 被加入 sum
sum += MR
sum += BL
sum += BC
sum += BR
val = 9 * center - sum   // = 9*center - (center + 8_neighbors) = 8*center - 8_neighbors
```
→ 依赖链深度: **load → 9 add → 1 fmsub → clamp → stream** (约 11 级)

**关键差异**: IPCC 将 9 个像素分成 4+1 个独立组，每组内部加法可并行执行，依赖链只有 2-3 级；本项目 9 次累加全部串行在 `sum` 寄存器上，形成 9 级加法依赖链。在 Arrow Lake 的宽发射架构上，IPCC 的 ILP 设计更好地利用了指令级并行。

#### 6.6.3 对齐处理对比

**IPCC**：一次性前置对齐。只在进入主循环前做一次标量对齐循环，将 `out` 指针对齐到 32 字节：
```cpp
int alignOffset = alignPtr - (out + width + 1);
for (int i = 0; i < alignOffset; ++i) { /* scalar */ }
// 之后所有 stream store 地址均对齐
```

**本项目**：每行独立对齐。在 `#pragma omp parallel for` 内每行都检测对齐：
```cpp
for (int i = 1; i < height; i++) {
    misalign = addr & 31;
    if (misalign != 0) { /* scalar prologue */ }
    // SIMD + stream store
}
```
→ 每行都执行分支判断 + 可能的标量循环，10410 列 × 5905 行 = 5905 次对齐检测。

#### 6.6.4 2x 循环展开效果分析

本项目 2x 展开理论上应减少循环开销 50%，但实际在本硬件上反而更慢。原因：

1. **寄存器压力**：2x 展开需要 ~14 个 YMM 寄存器（2×4 数据 + 3 常量 + 临时），接近 16 YMM 上限，编译器可能 spill 到栈
2. **指令缓存**：展开后循环体变大 (~100 条指令 vs IPCC 的 ~60 条)，可能超出 μop cache 容量
3. **乱序执行已足够**：Arrow Lake 的乱序窗口很大，1x 循环的 load latency 已被硬件预取和乱序执行隐藏，2x 展开的额外 ILP 无增量收益
4. **ICPC/ICC 编译器差异**：IPCC 原始优化报告使用 Intel 编译器 (ICC)，其自动向量化 + 循环展开策略与 GCC 不同

#### 6.6.5 公式等价性证明

两种公式数学等价：
```
本项目: 9 * center - (TL+TC+TR+ML+center+MR+BL+BC+BR)
       = 9*center - center - (TL+TC+TR+ML+MR+BL+BC+BR)
       = 8*center - sum_of_8_neighbors

IPCC:   8*center - (TL+TC+TR+ML+MR+BL+BC+BR)
       = 8*center - sum_of_8_neighbors
```
但本项目将 center 加入 sum 再 fmsub，多了一次依赖链上的加法。

#### 6.6.6 总结：为什么 IPCC 代码在本硬件更快

| 因素 | IPCC | 本项目 | 影响 |
|------|------|--------|------|
| 加法依赖链深度 | ~5 级 | ~11 级 | **关键差异** |
| 对齐开销 | 一次前置 | 每行一次 | 中 |
| 寄存器压力 | 低 (~8 YMM) | 高 (~14 YMM) | 中 |
| 指令缓存 | 紧凑 | 2x 展开体更大 | 低-中 |
| 软件预取 | 无 | 有 (T0 → L1) | 低（硬件预取已足够） |
| Load 数量 | 9 loads/8 elems | 9 loads/8 elems | 相同 |

**结论**：IPCC 的分组加法策略（将 9 个像素分入独立子树再合并）是关键优势，在本硬件上快约 7%。如果将 IPCC 的分组加法 + 本项目的 2x 展开组合，理论上可能进一步超越两者，但需注意寄存器压力。

---

## 七、尚未尝试的优化方向

### 高优先级

1. **P-core 精确绑定** ⭐⭐⭐
   - 当前 WSL2 无法区分 P-core/E-core
   - 在原生 Linux 上可用 `taskset -c <p-core-list>` 绑定
   - 预期额外 10-15% 提升

2. **AVX-512 版本** ⭐⭐⭐
   - Arrow Lake 是否支持 AVX-512 需确认
   - 如支持：一次处理 16 float，预期 1.5-2x SIMD 吞吐提升

### 中优先级

3. **大页内存** ⭐⭐
   - 减少 TLB miss（61M 像素 ≈ 244K 个 4KB 页）
   - `echo 1024 > /proc/sys/vm/nr_hugepages` + mmap MAP_HUGETLB

4. **多图像交叉验证** ⭐⭐
   - 题目要求用多个图像验证正确性
   - 已测试 IPCC.png → check.txt 完全一致

### 低优先级

5. **ICX/ICPX 编译器** ⭐
   - Intel oneAPI 编译器可能生成更好的代码
   - `icpx -O3 -xHOST -qopenmp -mavx2`

6. **2D cache tiling** ⭐
   - 行×列分块适配 L2 cache
   - 对超宽图像可能无明显收益

---

## 八、检查清单

- [x] 基准可编译运行，记录基线 8529.8ms (平台A) / 349.6ms (平台B)
- [x] O3 + OpenMP 并行化
- [x] AVX2 手动向量化（累加式加载 + fmsub + clamp）
- [x] Stream store 非临时写入（对齐处理）
- [x] 2x 循环展开 + 软件预取
- [x] Block kernel (读6算4) 实现并分析（确认不适用于超宽图像）
- [x] 最优线程数扫描 (平台A: 20T, 平台B: 16T)
- [x] 最优绑定策略 (平台A: spread, 平台B: P-core 12T close)
- [x] 输出 data.txt == check.txt 完全一致
- [x] 最终性能 ~88.9ms (平台B) / ~67.1ms (平台A)
- [x] IPCC 参考代码同硬件对比
- [x] IPCC vs 本项目实现差异详细分析
- [x] **多图像交叉验证** ✅ (5 images: IPCC.png + test_1024/2048/4096x2048/800x600)
- [x] **P-core 精确绑定** ✅ (平台B 原生 Linux: P-core 12T 优于全核 20T)
- [x] AVX-512 版本 ⭐⭐⭐（**不可行**：i9-13900H 和 Core Ultra 9 275HX 均无 AVX-512）
- [x] **引入 IPCC 分组加法策略** ✅ (依赖链 ~5 级，替代顺序累加 ~11 级)
- [x] **大页内存** ⚠️ (madvise MADV_HUGEPAGE 已添加，但 THP 未触发；显式 HugeTLB 需 root)

---

## 九、新增优化详解 (平台 B)

### 9.1 P-core 精确绑定 ⭐⭐⭐ (已完成)

原生 Linux 暴露了完整的 CPU hybrid 拓扑，可通过 `taskset` 精确绑定：

```
P-core (性能核): CPU 0-11 (6 核 × 2 线程)
E-core (能效核): CPU 12-19 (8 核 × 1 线程)
```

**测试结果 (stencil, 3 次取最优)**：

| 配置 | 时间 | 说明 |
|------|------|------|
| 20T all-core spread | ~93.8ms | 全部核心，E-core 拖累 |
| 16T all-core spread | 91.2ms | 16 线程，部分 E-core |
| P-core 12T spread | 90.3ms | 纯 P-core，spread 绑定 |
| **P-core 12T close** | **88.9ms** | ★ 最优！close 绑定 P-core |
| E-core 8T spread | 109.9ms | 纯 E-core，慢 23% |

**结论**：
- P-core 12T close 比全核 20T 快 ~5%（88.9ms vs 93.8ms）
- E-core 比 P-core 慢约 2.8x/每核（109.9ms/8E vs 90.3ms/12P）
- `OMP_PROC_BIND=close` 在 P-core 上优于 `spread`（可能与共享 L2 cache 有关）
- **推荐配置**: `OMP_NUM_THREADS=12 OMP_PROC_BIND=close taskset -c 0-11`

### 9.2 IPCC 分组加法策略 ⭐⭐ (已完成)

**原理**：将 9 点 stencil 的加法从顺序累加（~11 级依赖链）改为分组并行（~5 级依赖链）：

```
原始顺序累加:                    分组加法:
sum = TL                          val1 = TL + TC      (组1)
sum += TC   }                     val2 = TR + ML      (组2)
sum += TR   }                     val3 = 8*C - MR     (组3, fmsub)
sum += ML   } 9 级依赖链          val4 = BL + BC      (组4)
sum += C    }                     val5 = BR           (组5, load)
sum += MR   }
sum += BL   }                     合并 (3 级树):
sum += BC   }                     v10 = val1 + val2
sum += BR   }                     v11 = val3 - val4
                                   v20 = v10 + val5
val = 9*C - sum                   val = v11 - v20
```

**测试结果 (平台 B, 3 次取最优)**：

| 版本 | 16T spread | P-core 12T |
|------|-----------|------------|
| 分组加法 (stencil) | 91.2ms | **88.9ms** |
| 分组加法 (stencil_simple) | 91.9ms | **86.9ms** |
| 顺序累加 (stencil_avx2) | 93.8ms | 89.0ms |

**结论**：分组加法在平台 B 上带来约 1-2% 的轻微提升，远小于平台 A 上的 7% 差异。原因：i9-13900H (Raptor Lake) 的乱序执行窗口较 Arrow Lake 更窄，ILP 收益有限。但代码已整合，作为默认策略。

### 9.3 多图像交叉验证 ⭐⭐ (已完成)

使用 5 个不同尺寸/特征的测试图像验证正确性：

| 图像 | 尺寸 | 验证结果 | 耗时 (P-core 12T) |
|------|------|---------|-------------------|
| IPCC.png | 10410×5905 | ✅ opt == dbg | 88.9ms |
| test_1024.png | 1024×1024 | ✅ opt == dbg | 1.4ms |
| test_2048.png | 2048×2048 | ✅ opt == dbg | 4.5ms |
| test_4096x2048.png | 4096×2048 | ✅ opt == dbg | 10.6ms |
| test_800x600.png | 800×600 | ✅ opt == dbg | 0.8ms |

验证方法：优化版 (Ofast) 输出 vs 调试版 (O0) 输出逐元素对比，全部一致。

### 9.4 大页内存 ⭐⭐ (部分完成)

**已做**：在 `image.cc` 的 `malloc` 后添加 `madvise(ptr, size, MADV_HUGEPAGE)`，提示内核使用透明大页 (THP)。

**效果**：平台 B 上 THP 未实际触发（AnonHugePages=0），可能原因：
- malloc (brk) 分配的内存未满足 THP 对齐条件
- 61M 像素 × 4B = 244MB，分配跨越多个 2MB 边界

**待尝试**：使用 `posix_memalign` + `MAP_HUGETLB` + root 预分配大页。在无 root 环境中 MADV_HUGEPAGE 是最佳努力。

### 9.5 stencil_simple 正确性修复

**Bug 根因**：原始 stencil_simple.cc 的手动线程划分公式存在舍入溢出：
- `countPerThread = (counts / threadsNum + 7) & (~7)` 向上舍入
- 20 线程 × 每线程 3072512 = 61450240 > counts (61450228)
- 最后 12 个元素越界访问，导致数据损坏

**修复**：改用 `#pragma omp parallel for` + row-by-row 迭代，消除手动划分。

---

## 十、平台对比分析

| 项目 | 平台 A (Arrow Lake) | 平台 B (Raptor Lake) | 比值 |
|------|--------------------|--------------------|------|
| CPU | Core Ultra 9 275HX | Core i9-13900H | — |
| 系统 | WSL2 | 原生 Linux | — |
| 编译器 | GCC 9.4.0 | GCC 11.4.0 | — |
| P-core 拓扑 | 不可见 (WSL2) | 可见，可绑定 | — |
| 最优时间 | 62.5ms (IPCC ref) | 86.9ms (IPCC grouped) | 1.39× |
| 20T 全核时间 | 67.1ms | 93.8ms | 1.40× |
| 分组加法效果 | 7% 提升 | 1-2% 提升 | 差异较大 |
| 每核效率 | 1336 core-ms | 1043 core-ms | 1.28× |

**分析**：平台 B 比平台 A 慢约 40%。主要原因：
1. **微架构差异**：Arrow Lake 的 P-core (Lion Cove) IPC 高于 Raptor Lake (Raptor Cove)
2. **编译器差异**：GCC 11 vs GCC 9，自动向量化策略不同
3. **WSL2 vs 原生**：WSL2 有虚拟化开销，但 Arrow Lake 的架构优势弥补了这一点
4. **内存子系统**：Arrow Lake 的 L2 cache 更大（单核更多），有利于超宽图像
